<h2>{{ labels.title }}</h2>
<div class="form_field">
    <input
            id="map_key"
            placeholder="{{ labels.map_key }}"
            name="brander-map-settings[map_key]"
            type="text"
            class="input"
            value="{{ options.map_key }}"
    />
    <label for="sender_name">{{ labels.sender_email }}</label>
</div>
<div class="submit">
    <input type="submit" value="{{ labels.save_text }}" />
</div>